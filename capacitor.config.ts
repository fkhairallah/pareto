import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deligent.pareto',
  appName: 'pareto',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
